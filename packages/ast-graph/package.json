{
  "name": "@infinity-canvas/ast-graph",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace indexer + dependency graph builder (file-level imports first)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src/",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "@infinity-canvas/schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
