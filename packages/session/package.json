{
  "name": "@infinity-canvas/session",
  "version": "0.0.0",
  "private": true,
  "description": "Session state management + cache (.infinity-canvas/)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src/",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "@infinity-canvas/ast-graph": "workspace:*",
    "@infinity-canvas/detail-pane": "workspace:*",
    "@infinity-canvas/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
