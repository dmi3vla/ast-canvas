{
  "name": "@infinity-canvas/semantic",
  "version": "0.0.0",
  "private": true,
  "description": "LLM context packer + codemap builder for semantic map generation",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src/",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "@infinity-canvas/ast-graph": "workspace:*",
    "@infinity-canvas/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
