{
  "name": "@infinity-canvas/schema",
  "version": "0.0.0",
  "private": true,
  "description": "Zod schemas + TypeScript types: CanvasDocument, Codemap, DepGraph",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src/",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
